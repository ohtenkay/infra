return {
  'saghen/blink.pairs',
  enabled = false,
  version = '*', -- (recommended) only required with prebuilt binaries
  dependencies = 'saghen/blink.download', -- prebuilt binaries
  --- @module 'blink.pairs'
  --- @type blink.pairs.Config
  opts = {},
}
